<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Storage.App.ViewModels"
             xmlns:controls="clr-namespace:Storage.App.Controls"
             x:Class="Storage.App.Views.AddItemPage"
             x:DataType="vm:AddItemViewModel"
             Title="{Binding PageTitle}">

    <!-- Fields scroll; the action buttons stay pinned to the bottom. -->
    <Grid RowDefinitions="*,Auto">

        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="20" Spacing="15">

                <!-- Photo. Empty state is a tappable placeholder; filled state is a
                     thumbnail with the two actions that apply to it. -->
                <Label Text="Photo" FontSize="16" />

                <HorizontalStackLayout Spacing="15" IsVisible="{Binding HasPhoto}">
                    <Border WidthRequest="120"
                            HeightRequest="120"
                            Padding="0"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10" />
                        </Border.StrokeShape>
                        <Image Source="{Binding PhotoSource}" Aspect="AspectFill" />
                    </Border>

                    <VerticalStackLayout Spacing="10" VerticalOptions="Center">
                        <Button Text="Replace" Command="{Binding TakePhotoCommand}" />
                        <Button Text="Remove"
                                Command="{Binding RemovePhotoCommand}"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                                TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>

                <HorizontalStackLayout Spacing="15" IsVisible="{Binding HasPhoto, Converter={StaticResource InvertedBoolConverter}}">
                    <Border WidthRequest="120"
                            HeightRequest="120"
                            Padding="0"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray600}}"
                            Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray500}}">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10" />
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding TakePhotoCommand}" />
                        </Border.GestureRecognizers>
                        <Label Text="📷"
                               FontSize="36"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               SemanticProperties.Description="No photo yet" />
                    </Border>

                    <VerticalStackLayout Spacing="10" VerticalOptions="Center">
                        <Button Text="Take photo" Command="{Binding TakePhotoCommand}" />
                        <Button Text="Choose from gallery"
                                Command="{Binding ChooseFromGalleryCommand}"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                                TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>

                <Label Text="Item Name" FontSize="16" Margin="0,10,0,0" />
                <Entry Text="{Binding Name}"
                       Placeholder="Enter item name"
                       MaxLength="200" />

                <Label Text="Quantity" FontSize="16" Margin="0,10,0,0" />
                <controls:NumericStepper Value="{Binding Quantity}" Minimum="1" />

                <Label Text="Location (Optional)" FontSize="16" Margin="0,10,0,0" />
                <Picker ItemsSource="{Binding Locations}"
                        SelectedItem="{Binding SelectedLocation}"
                        ItemDisplayBinding="{Binding Name}"
                        Title="Select a location" />

                <Label Text="Description (Optional)" FontSize="16" Margin="0,10,0,0" />
                <Editor Text="{Binding Description}"
                        Placeholder="Enter description"
                        HeightRequest="100"
                        MaxLength="1000" />

            </VerticalStackLayout>
        </ScrollView>

        <Grid Grid.Row="1"
              ColumnDefinitions="*,*"
              ColumnSpacing="10"
              Padding="20,10">
            <Button Grid.Column="0"
                    Text="Cancel"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                    TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
            <Button Grid.Column="1"
                    Text="Save"
                    Command="{Binding SaveItemCommand}" />
        </Grid>

    </Grid>

</ContentPage>
